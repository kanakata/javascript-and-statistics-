export class Matrices {
  add(array) {
    const array_length = array.length;
    let result = [];

    while (result.length < array[0].length) {
      for (let i = 0; i < array.length; i++) {}
    }

    return result;
  }
}
