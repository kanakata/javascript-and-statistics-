export class Algebra {
  lcm(numbers) {
    const factors = [];
    for (let i = 0; i < numbers.length; i++) {
      const n_factors = [];
      for (let j = 0; j < numbers[i].length; j++) {
        n_factors.push();
      }
    }
  }

  factors(number) {
    const factors = [];
  }
}
